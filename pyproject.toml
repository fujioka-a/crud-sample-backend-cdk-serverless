[tool.poetry]
name = "crud-sample-backend-cdk-serverless"
version = "0.1.0"
description = ""
authors = ["fujioka"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
